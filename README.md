# -Getting-Multiple-Tables-from-Excel-into-Power-Query
This project demonstrates how to import multiple tables from an Excel file into Power Query, clean them, and combine (append) them into a single structured dataset.  The goal is to eliminate manual copy-paste work and create an automated, refreshable data process using Power Query.
